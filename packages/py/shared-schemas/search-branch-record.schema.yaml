$schema: "https://json-schema.org/draft/2020-12/schema"
title: SearchBranchRecord
type: object
required:
  - branch_id
  - hypothesis
  - attempt
  - score
  - score_direction
  - status
properties:
  branch_id:
    type: string
  parent_branch_id:
    type:
      - string
      - "null"
  workflow_id:
    type: string
  phase_id:
    type: string
  hypothesis:
    type: string
  attempt:
    type: integer
    minimum: 1
  score:
    type: number
    description: Relative branch ranking value within the current searchable node
  score_direction:
    type: string
    enum:
      - higher_is_better
      - lower_is_better
    description: Declares how score should be interpreted during branch selection
  status:
    type: string
    enum:
      - proposed
      - running
      - failed
      - superseded
      - selected
      - discarded
  failure_reason:
    type:
      - string
      - "null"
  artifacts:
    type: array
    items:
      type: string
  selected:
    type: boolean
  created_at:
    type: string
    format: date-time
  closed_at:
    type:
      - string
      - "null"
    format: date-time
