# Proto

Put gRPC contracts here when a service boundary is mature enough to justify an internal RPC interface.

Default rule:
- use FastAPI modules first
- add gRPC only for independently deployable internal services
