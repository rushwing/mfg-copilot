# Markdown Planning

Use Markdown Planning when a workflow needs human readability, approval checkpoints, or durable step-by-step state that can be audited later.

Best fit in this repo:
- `services/api-gateway/` for approval-facing plan rendering
- `services/orchestrator/` for plan persistence and execution progress
- `services/agents/doc-copilot/` for structured drafts and checklists
