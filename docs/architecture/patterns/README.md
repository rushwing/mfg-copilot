# Agent Patterns

Architecture-facing notes for reusable agent execution patterns.

- `react.md`: think-act-observe loops for tool calling
- `markdown-planning.md`: explicit plan serialization for reviewability
- `phase-graph-orchestration.md`: phase graph outside, search tree inside, ReAct per branch
- `pr-review-packet-visual-language.md`: reviewer-facing packet section order, table conventions, and visual rules
- `pr-review-packet-example.md`: canonical reviewer-facing packet example
- `ralph-loop.md`: iterative critique and refinement loop for higher-risk execution
- `structured-generation.md`: schema-first machine-consumed outputs with constrained decoding
