# ReAct

Use ReAct as the default pattern for operational agent workflows that need to reason over state, call tools, inspect tool output, and continue with controlled iteration.

Best fit in this repo:
- `services/agents/ops-agent/`
- `services/agents/deploy-agent/`
- shared helpers in `packages/py/agent-patterns/`
