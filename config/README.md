# Config

Configuration that should be versioned and reviewed lives here.

- `env/`: template env files
- `model-routing/`: SGLang vs remote model routing rules
- `policies/`: approval, safety, and execution policy definitions
